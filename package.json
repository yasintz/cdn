{
  "name": "generic",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://yasintz.github.io/cdn",
  "scripts": {
    "build:projects": "rm -rf dist/projects && mkdir dist/projects && cp -r projects/* dist/projects",
    "build:sudoku": "rm -rf dist/projects && mkdir dist/sudoku && node sudoku/build.js"
  },
  "dependencies": {
    "classnames": "^2.3.2",
    "esbuild": "^0.17.0",
    "node-sass": "^8.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
