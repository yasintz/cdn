/** @type {import('tailwindcss').Config} */
// Note: Most configuration has been migrated to CSS using @theme in globals.css
// This file is kept for compatibility with tools like shadcn/ui
module.exports = {
  darkMode: ['class'],
  // Content scanning is now handled via @source directives in globals.css
  content: [],
  // Theme configuration is now in CSS using @theme directive
  theme: {},
  // Plugin is loaded via @plugin directive in globals.css
  plugins: [],
};
