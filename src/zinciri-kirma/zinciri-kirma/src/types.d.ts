export type DataType = {
  title: string;
  items: Array<{
    text: string;
    bottomText: string;
  }>;
};
export type StyleType = {
  color: string;
  fontSize: number;
};
export interface OffsetType {
  x: number;
  y: number;
}

